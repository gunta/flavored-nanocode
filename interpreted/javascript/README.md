# JavaScript Nanocode

## Node.js (zero deps)

```bash
node node/nanocode.mjs
```

## Browser

Open `browser/nanocode.html` in a browser. Uses localStorage as filesystem.

**Note:** Requires CORS headers. Use Anthropic API key directly (will prompt on first load).
